[package]
name = "provenance-acceptance"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
provenance-domain = { path = "../../crates/provenance-domain" }
provenance-core = { path = "../../crates/provenance-core" }
provenance-adapters = { path = "../../crates/provenance-adapters" }

[lints]
workspace = true
