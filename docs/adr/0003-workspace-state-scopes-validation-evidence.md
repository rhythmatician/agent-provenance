---
status: accepted
---

# Scope validation evidence to workspace state

A validation result applies only to the `Workspace State` against which it executed. A later state makes that evidence stale unless content identity proves equivalence; an `Observation Gap` that breaks workspace continuity makes freshness indeterminate. A passing command is therefore never treated as timeless proof that the final workspace was validated.
